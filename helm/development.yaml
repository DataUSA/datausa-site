image:
  pullPolicy: Always

imagePullSecrets:
  - name: github

replicaCount: 1

autoscaling:
  enabled: true
  minReplicas: 1
  maxReplicas: 10
  targetCPUUtilizationPercentage: 80
  targetMemoryUtilizationPercentage: 120

resources:
  requests:
    cpu: 250m
    memory: 2Gi
  limits:
    cpu: 500m
    memory: 4Gi

livenessProbe:
  failureThreshold: 3
  httpGet:
    path: /
    port: 3300
    scheme: HTTP
  initialDelaySeconds: 40
  periodSeconds: 30
  timeoutSeconds: 30
  successThreshold: 1

readinessProbe:
  failureThreshold: 3
  httpGet:
    path: /
    port: 3300
    scheme: HTTP
  initialDelaySeconds: 40
  periodSeconds: 30
  timeoutSeconds: 30
  successThreshold: 2

service:
  type: ClusterIP
  port: 3300

serviceAccount:
  # Specifies whether a service account should be created
  create: true
  # Annotations to add to the service account
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name: ""

configMap:
  CANON_API: "https://app-dev.datausa.io"
  CANON_CMS_CUBES: "https://arkansas-api.datausa.io/"
  CANON_CMS_ENABLE: "true"
  CANON_CMS_FORCE_HTTPS: "true"
  CANON_CMS_GENERATOR_TIMEOUT: "600000"
  CANON_CMS_LOGGING: "true"
  CANON_CMS_MINIMUM_ROLE: "1"
  CANON_CMS_REQUESTS_PER_SECOND: "60"
  CANON_CONST_CART: "datausa-cart-v3"
  CANON_CONST_CUBE: "https://arkansas-api.datausa.io/"
  CANON_CONST_TESSERACT: "https://api-dev.datausa.io/tesseract/"
  CANON_DB_HOST: "104.154.115.126"
  CANON_DB_NAME: "datausa-cms-21"
  CANON_DB_USER: "deploy"
  CANON_GEOSERVICE_API : "https://geoservice.datausa.io/"
  CANON_GOOGLE_ANALYTICS: "UA-70325841-1"
  CANON_LANGUAGES: "en"
  CANON_LANGUAGE_DEFAULT: "en"
  CANON_LOGICLAYER_CUBE: "https://app-arkansas-api.datausa.io/"
  CANON_LOGICLAYER_SLUGS: "true"
  CANON_LOGINS: "true"

secrets:
  CANON_DB_PW: TEpLcndzWDc5WTg3ZWVxTUdNemdjd0E2bVZyQ3Rp
  CANON_HOTJAR: MTI1NzU3MA==

ingress:
  enabled: true
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    acme.cert-manager.io/http01-edit-in-place: "true"
    ingress.kubernetes.io/ssl-redirect: "false"
    nginx.org/proxy-connect-timeout: "480s"
    nginx.org/proxy-read-timeout: "480s"
    nginx.org/proxy_send_timeout: "480s"
    nginx.org/proxy-buffers: "8 16k"
    nginx.org/proxy-buffer-size: "16k"
    nginx.org/proxy-busy-buffers-size: "64k"
    nginx.org/websocket-services: "canon-site"
    nginx.org/location-snippets: |
      add_header Access-Control-Allow-Origin *;
    nginx.org/server-snippets: |
      if ($scheme != "https") {
          return 308 https://$host$request_uri;
      }
  hosts:
    - host: app-dev.datausa.io
      paths:
        - /
        - /ws
  tls:
    - secretName: canon-site-ingress-tls
      hosts:
        - app-dev.datausa.io
